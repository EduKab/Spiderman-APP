import 'package:flutter/material.dart';

class AlertEditCalendar extends StatefulWidget {
  const AlertEditCalendar({super.key});

  @override
  State<AlertEditCalendar> createState() => _AlertEditCalendarState();
}

class _AlertEditCalendarState extends State<AlertEditCalendar> {
  @override
  Widget build(BuildContext context) {
    return AlertDialog();
  }
}